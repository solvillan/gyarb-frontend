<?xml version="1.0" encoding="utf-8" ?>
<!DOCTYPE :noop SYSTEM "../lml.dtd">
<:noop>
    <:log>Init menu window</:log>
    <window>
        <window>
            <label>$getName</label>
            <!-- More info, like statistics and such -->
        </window>
        <window>
            <label>Friends</label>
            <list id="friends">
            </list>
        </window>
        <window>
            <table onecolumn="true">
                <label>Players</label>
                <textfield message="Search" />
                <list id="players">
                </list>
            </table>
        </window>
    </window>
</:noop>